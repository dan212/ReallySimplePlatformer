#version 330 core

in vec3 fragmentColor;
//uniform samplerBuffer Buffer;
// Ouput data
out vec4 color;

void main()
{
	// Output color = red;
	color = vec4(fragmentColor[0],fragmentColor[1],fragmentColor[2],1.0);
}